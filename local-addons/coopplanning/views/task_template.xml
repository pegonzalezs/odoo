<odoo>
    <record model="ir.ui.view" id="task_template_view_tree">
        <field name="name">Task Template List</field>
        <field name="model">coopplanning.task.template</field>
        <field name="arch" type="xml">
            <tree>
                <field name="task_type_id" />
                <field name="name" />
                <field name="day_nb_id" />
                <field name="start_time"
                       widget="float_time" />
                <field name="end_time"
                       widget="float_time" />
                <field name="duration"
                       widget="float_time" />
                <field name="worker_nb" />
            </tree>
        </field>
    </record>

    <record model="ir.ui.view" id="task_template_view_form">
        <field name="name">Task Template Form</field>
        <field name="model">coopplanning.task.template</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button type="object" name="generate_task" string="Generate Task for Today" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="task_type_id" options="{'no_create': true}" required="1" />
                            <field name="day_nb_id" options="{'no_create': true}" required="1" />
                            <field name="worker_nb" />
                            <field name="active" />
                        </group>
                        <group>
                            <field name="start_time"
                                   widget="float_time" />
                            <field name="end_time"
                                   widget="float_time"  />
                            <field name="duration"
                                   widget="float_time"  />
                            <field name="floating" />
                        </group>
                    </group>
                        <separator string="Pre defined Worker" />
                        <field name="worker_ids" nolabel="1"
                               attrs="{'readonly': [('floating', '=', True)]}">
                             <tree>
                                <field name="name" />
                                <field name="phone" />
                                <field name="email" />
                                <field name="lang" />
                                <field name="category_id" widget="many2many_tags"/>
                            </tree>
                        </field>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Actions -->
    <record model="ir.actions.act_window" id="action_task_template">
      <field name="name">Task Template Action</field>
      <field name="res_model">coopplanning.task.template</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- Top menu item -->
    <menuitem name="Coop Planning (M03E03)" id="menu_root"/>
    <!-- menu categories -->
    <menuitem name="Templates" id="menu_template_top" parent="menu_root" sequence="2"/>
    <!-- actions -->
    <menuitem name="Task" id="menu_task_template" parent="menu_template_top"
              action="action_task_template"/>
</odoo>
